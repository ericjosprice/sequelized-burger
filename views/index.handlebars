
<h1>Burgers</h1>

<h2>Burgers to be Eaten</h2>


<ul>
  {{#each burgers}}
  {{#unless devoured}}
  <li>
    <p>{{burger_name}}</p>
    <button class="burger-devoured" data-id="{{this.id}}" data-newsleep="{{devoured}}">
      Devour
    </button>
  </li>
  {{/unless}}
  {{/each}}
</ul>

<h2>Eaten Burgers</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  <li>{{burger_name}}</li>
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="newBurger">Burger Name:</label>
    <input type="text" id="newBurger" name="name">
  </div>
  <button type="submit">Add a Burger</button>
</form>